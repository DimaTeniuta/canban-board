import React from 'react';
import Register from '../../features/Register/Register';

const RegisterPage = () => {
  return (
    <main>
      <Register />
    </main>
  );
};

export default RegisterPage;
